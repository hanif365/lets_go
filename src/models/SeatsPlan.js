// import mongoose from 'mongoose';

// const seatSchema = new mongoose.Schema({
//   seatId: Number,
//   isBooked: Boolean,
// });

// const busSchema = new mongoose.Schema(
//   {
//     busNumber: String,
//     seatsData: [seatSchema],
//   },
//   { timestamps: true }
// );

// export default mongoose.models.SeatsPlan || mongoose.model('SeatsPlan', busSchema);
