export const metadata = {
  title: "Let's Go Event Ticket",
  description: "Payment Success, Now Please print out your ticket",
};

export default function SuccessPageLayout({ children }) {
  return <section>{children}</section>;
}
